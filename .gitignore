
# ──── Randi: secrets protection (auto-generated) ────
# Service account keys
*-sa-key.json
*-sa-key-*.json
*service-account*.json
*serviceaccount*.json
*credentials*.json
gcp-*.json
firebase-*.json

# Environment & config secrets
.env
.env.*
!.env.example
!.env.sample
!.env.template

# API key files & agent config
opencode.json
.opencode/
AGENTS.md

# Git credentials (should never be in workspace, but just in case)
.git-credentials
.gitcredentials
.netrc

# Randi internal
.hive/
.context/
_secrets/

# Common secret file patterns
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa*
id_ed25519*
*.gpg

# ──── Build artifacts & dependencies (auto-generated) ────
# CRITICAL: Never commit these — they are huge and cause OOM kills
node_modules/
.npm/
.pnpm-store/
.yarn/
bower_components/
jspm_packages/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.eggs/
*.egg-info/

# Build outputs
dist/
build/
out/
.next/
.nuxt/
.svelte-kit/
.cache/
.parcel-cache/
.turbo/
.vercel/

# IDE & OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~
previous_output/
.sources/

# ──── Randi: secrets protection (auto-generated) ────
# Service account keys
*-sa-key.json
*-sa-key-*.json
*service-account*.json
*serviceaccount*.json
*credentials*.json
gcp-*.json
firebase-*.json

# Environment & config secrets
.env
.env.*
!.env.example
!.env.sample
!.env.template

# API key files & agent config
opencode.json
.opencode/
AGENTS.md

# Git credentials (should never be in workspace, but just in case)
.git-credentials
.gitcredentials
.netrc

# Randi internal
.hive/
.context/
_secrets/

# Common secret file patterns
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa*
id_ed25519*
*.gpg

# ──── Build artifacts & dependencies (auto-generated) ────
# CRITICAL: Never commit these — they are huge and cause OOM kills
node_modules/
.npm/
.pnpm-store/
.yarn/
bower_components/
jspm_packages/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.eggs/
*.egg-info/

# Build outputs
dist/
build/
out/
.next/
.nuxt/
.svelte-kit/
.cache/
.parcel-cache/
.turbo/
.vercel/

# IDE & OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# ──── Randi: secrets protection (auto-generated) ────
# Service account keys
*-sa-key.json
*-sa-key-*.json
*service-account*.json
*serviceaccount*.json
*credentials*.json
gcp-*.json
firebase-*.json

# Environment & config secrets
.env
.env.*
!.env.example
!.env.sample
!.env.template

# API key files & agent config
opencode.json
.opencode/
AGENTS.md

# Git credentials (should never be in workspace, but just in case)
.git-credentials
.gitcredentials
.netrc

# Randi internal
.hive/
.context/
_secrets/

# Common secret file patterns
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa*
id_ed25519*
*.gpg

# ──── Build artifacts & dependencies (auto-generated) ────
# CRITICAL: Never commit these — they are huge and cause OOM kills
node_modules/
.npm/
.pnpm-store/
.yarn/
bower_components/
jspm_packages/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.eggs/
*.egg-info/

# Build outputs
dist/
build/
out/
.next/
.nuxt/
.svelte-kit/
.cache/
.parcel-cache/
.turbo/
.vercel/

# IDE & OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~
